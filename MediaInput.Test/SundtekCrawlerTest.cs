/*
using NUnit.Framework;

namespace MediaInput.Test
{
    class SundtekCrawlerTest
    {
        [SetUp]
        public void Setup()
        {
        }

        [Test]
        public void Test()
        {
            /*TODO: Figure out a way to mock the connector (maybe with a custom connection string that is injected into the config?) to test the crawler
            SundtekCrawler crawler = SundtekCrawler.GetSingleton(new System.Net.Http.HttpClient());

            crawler.StartCrawling(TimeSpan.FromMinutes(10));

            System.Threading.Thread.Sleep(600000);
            #1#
        }
    }
}
*/
